"""
Should we have ways (for examnpel copulas) to build multivariate distributions from univariate ones?
Or should we just have a multivariate distribution class that takes care of everything?
I guess the latter is more flexible, but the former could be useful for some specific cases.
"""

from dalia.statistical_modeling_toolbox.distributions import Distribution


class MultivariateDistribution(Distribution):
    """
    ...
    """

    # 1. Class attributes (if any)
    # 2. Initialization
    # 3. Special representation methods
    # 4. Properties (grouped together)
    # 5. Comparison operators (if needed)
    # 6. Arithmetic operators (standard order)
    # 7. Right-hand operators (same order as above)
    # 8. In-place operators (if supported)
    # 9. Other special methods
    # 10. Public methods
    # 11. Private/protected methods (start with _)
